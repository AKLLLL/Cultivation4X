# Cultivation4X 项目当前状态（2026-08-25 更新）

本文档记录世界地图与旧玩法融合、资源生态、弟子个性自主行为、炼气期纳气成长和功法系统的当前可运行基线。这里是阶段性实现事实，不是冻结架构。

## 1. 概览

- 当前分支：`agent/influence-integration`，远端：`origin`。
- 当前里程碑：资源月结、弟子 Utility AI、30 日循环计划模板、练气三层纳气与功法修炼进入同一条 `TimeManager → NPC/Mission/Event → 夜间泄露/月结 → 日结/存档` 流程。
- 存档版本：`SaveDataVersion.Current = 21`；不兼容旧档，本版从新档测试。
- 本次没有修改 `WorldGenerator`、`TerrainRenderer`、Camera；SampleScene 仅由 Unity 重序列化新的计划字段，UIRoot 与月计划 Prefab 已更新。

## 2. 已完成系统

### 2.1 世界地图与资源生态

- `WorldMapData / WorldCell / HexGeometry / Terrain` 是唯一地图数据源；`MapSiteData` 保存地点玩法状态，`WorldLocation` 只作为地图与交互门面。
- `WarehouseData.items` 是物品库存唯一真源，所有增减通过 `WarehouseManager`。
- 地图资源节点每 30 天结算一次，`lastSettledMonth` 防止重复结算；当日刚开发节点从下月开始产出。
- 宗门事务按实际执行人日累计：每 5 人日产出 1 基础材料，余数持久化在 `sectDutyWorkCredit`。
- 已开发灵泉不再每日直加修为；它只在弟子执行修炼日时提供环境吸收效率 +10%。

### 2.2 弟子个性与自主行为

- `DiscipleDecisionManager` 在日结后调度，Goal/Utility 评分位于 `DiscipleAIEvaluator`，执行统一桥接到既有 Mission。
- 个性、特质、能力、资源环境和关系目标影响弟子选择；AI 不直接写 Mission 奖励、关系或纳气进度。
- 自主多日 Mission 只能在未来连续自由日足够且不跨月时启动。
- 玩家 Mission 优先于日程；被占用的计划日不补做、不改写，取消 AI Mission 不触发失败惩罚。
- AI 能力评分中的旧资质入口已改为灵根品质；旧角色等级/经验成长入口和 `NPCGrow` 已移除。

### 2.3 炼气期纳气模型

- 练气成长状态真源位于 `CharacterState`：`realmLayer`、`naqiProgress`、`currentAura`、`auraControl`、`fatigue`、`spiritRoot`；个人功法状态由同一角色上的 `mainTechniqueId`、`activeAuxiliaryTechniqueIds`、`techniqueProgresses` 保存。
- 炼气共三层，每层纳气 0–100；溢出自动带入下一层，第三层 100 为炼气圆满。圆满后的修炼日仍可练习灵气控制，但不再增加纳气。
- 灵根负责吸收、泄露与小幅炼化修正：

| 品质 | 吸收倍率 | 夜间泄露 | 炼化倍率 |
|---|---:|---:|---:|
| 驳杂 | 0.2 | 95% | 0.90 |
| 下品 | 0.6 | 90% | 0.95 |
| 中品 | 1.0 | 70% | 1.00 |
| 上品 | 1.8 | 50% | 1.05 |
| 极品 | 3.0 | 30% | 1.10 |
| 天灵根 | 6.0 | 0% | 1.15 |

- 五行亲和度归一化后合计为 1；当前只用于主修功法的柔性适配倍率（0.9–1.1），未扩展元素克制系统。
- 根骨只决定灵气承载。练气一/二/三层基础承载为 50/75/100，再乘孱弱 0.7、普通 1.0、优秀 1.3、上佳 1.6、天生道体 2.0；容量为派生值，不写入存档。
- 只有修炼日吸收环境灵气；所有日子在事件处理后进行夜间泄露，未泄露部分跨日保留。
- 修炼日从打坐炼化、基础练功、灵气运转尝试中稳定加权选择。性格、疲劳与控制力影响选择；运转失败为软失败，只保留 25% 收益。
- 疲劳是软限制，最高只降低约 30% 炼化效率；每日先恢复，再根据行为增加。
- 标准中品灵根、普通根骨、中性功法适配、每月 15 个修炼日的目标仍是 85–100 个日历日完成炼气三层。
- 旧当日灵气归零、大周天、灵气紊乱、调息丹、修炼灵石辅助和 Mission `expReward` 路径已移除。

### 2.4 功法系统 V1

- `Assets/Resources/Configs/Techniques/techniques.json` 是功法定义真源；当前有《青木养气诀》《赤阳锻气诀》《太虚导引篇》三门主修功法，配置包含五行倾向、吸收/炼化/稳定修正和行动标签偏好。
- `PlayerData.techniqueLibrary` 保存宗门功法库、宗门推演进度和批注选择；个人理解进度只保存在对应 `CharacterState.techniqueProgresses`，二者不互相代替。
- 立宗选择功法时，功法进入宗门功法库，并由三名创始弟子学习、装备为主修；以后招募的弟子不会自动学会。
- 个人理解阶段按 0/30/60/90 划分为初学、熟练、精通、圆满。切换主修保留原功法进度；辅修槽目前只保存状态，不产生玩法效果。
- 修炼日同时结算个人理解和宗门推演贡献；Mission/Event 的功法奖励只增加执行者当前主修的个人理解，不增加宗门推演。
- 宗门推演首次到 30 时封顶并触发对应功法的首次批注事件；选择“入门批注”或“因材批注”后解除封顶。60/90 阶段当前自动晋级，不触发额外事件。
- 入门批注在个人理解低于 30 时提供 25% 学习修正；因材批注在对应灵根亲和不低于 0.4 时提供 20% 学习修正。每门功法 V1 只能选择一次首次批注。
- 功法对炼气容量没有影响；主修的吸收、炼化、稳定和五行适配只参与既有纳气公式。旧的单一 `techniqueMastery` 与立宗功法固定属性加成已被替换。
- Utility AI 通过行动标签读取当前主修的偏好评分；没有为功法另建一套 AI 或执行系统。
- 立宗教程中的“传承整理进度”与长期宗门推演是两套不同语义：前者只用于完成立宗引导，后者从 0 开始随实际修炼积累。

### 2.5 30 日循环计划模板

- `PlayerData.monthlyPlanTemplates` 是唯一真源；每个模板保存稳定 ID、名称、30 个循环日程格和绑定弟子 ID。
- 每名弟子最多绑定一个模板，多名弟子可以共享同一模板；未绑定弟子按自由活动执行，删除模板即解除其绑定。
- 第 1/31/61 天均读取模板第 1 格。月计划界面先选择修炼/宗务/自由画笔，再通过单击或按住拖过多个日期连续涂写；不提供整表批量覆盖入口。
- 绑定区默认只显示已绑定弟子；“添加弟子”展开候选列表，选择后加入绑定区，已绑定其他模板的弟子会转绑。
- 手动 Mission 和进行中的 Mission 优先于当日日程，不修改原计划，也不在以后补做。
- AI 多日 Mission 暂时仍不能跨越月末，这是既有任务边界，不由循环模板改变。

### 2.6 事件与展示

- 事件不再直接增加纳气：灵雨增加当前灵气，功法心得增加执行者当前主修的个人理解，比试、探索和修行经历增加灵气控制。
- 日结保存并展示当日最新修炼结果；人生履历只记录失败、进入新层和炼气圆满等里程碑。
- 弟子中心、弟子详情、列表、立宗候选和日结已改为显示灵根、练气层、当前灵气/容量、控制、疲劳与当日行为。
- 全局 HUD 首次绑定主菜单/角色创建状态时不再清空 `UIManager` 面板；只有确实从世界地图离开时才清理旧窗口，避免删档启动后立宗面板被初始化顺序关闭。

## 3. 时间与数据流

```text
结束今天
  → CurrentDay + 1
  → 弟子恢复疲劳/心境
  → Mission 占用检查
  → 未被 Mission 占用时执行当日日程（修炼同时结算个人理解与宗门推演；或宗务/自由）
  → Mission 推进
  → 外部威胁与 Event
  → 所有存活弟子夜间灵气泄露
  → 每 30 天资源月结
  → DaySettlementSummary
  → 自动存档
  → 日结后 DiscipleDecisionManager 选择下一项自主 Mission
```

## 4. 验证状态

| 项目 | 当前结果 |
|---|---|
| JSON 配置解析 | Unity 完整回归加载并通过配置校验 |
| `git diff --check` | 通过 |
| Unity 编译 | Assembly-CSharp / Assembly-CSharp-Editor 0 错误 |
| EditMode | 本轮完整回归 **479/479 通过**，failed=0，skipped=0；功法聚焦 **6/6**，SectVitality 聚焦 **12/12**，WorldMap3DController 聚焦 **12/12** |
| Play Mode | 新档同一会话“候选弟子 → 功法 → 宗门确认 → 选址”通过；选址地形、六角网格、格子点击、建立宗门及切回正式世界地图均正常 |

本轮测试证据：`Logs/sect-placement-full-results.xml`、`Logs/sect-placement-reactivation-results.xml`、`Logs/technique-system-focused-results-2.xml`、`Logs/technique-sect-vitality-results.xml`（本地日志，不提交）。

## 5. 当前高风险与限制

1. `CharacterState`、宗门功法库、事件与 Mission 奖励结构已改，V21 必须从新档开始；不提供旧版本迁移玩法保证。
2. 根骨品质目前由既有 `physique` 数值区间映射，尚无独立根骨品质字段；如果角色属性生成区间变化，必须同步复核容量映射。
3. 85–100 日是标准模板的确定性回归目标，不代表所有性格、灵根、环境与事件组合都落在该区间。
4. 功法五行适配、行动偏好、个人理解速度和宗门 30 阶段事件频率仍需 Play Mode 长周期观察；0.9–1.1 的柔性适配和 30–45 日首次批注目标是首版平衡基线。
5. 月计划已迁移为全局 Prefab Screen；1920×1080、1280×720 的自动布局和最终视觉密度仍需 Play Mode 手测。
6. `NPCData.level/exp` 仍作为旧 ScriptableObject 模板兼容字段存在，但已不参与运行态成长；删除公开序列化字段需单独审查资源引用。
7. 功法管理 UI 当前只展示主修、个人阶段、宗门推演和批注结果；未提供完整换功法/辅修管理界面，辅修也尚未接入实际效果。
8. 60/90 宗门阶段目前只自动晋级，尚无专属内容事件；不得把 V1 行为误当成长期冻结规则。
9. SampleScene 旧按钮/旧物件仍由 `LegacyWorldUiGate` 隐藏；清理时必须同时修改场景与 gate。

## 6. Play Mode 手测清单

1. 新建、复制、重命名和删除模板；绑定三名弟子，点击与拖动修改日程，并确认第 1/31/61 天循环一致。
2. 手动 Mission 覆盖修炼/宗务日且不补做；AI 多日 Mission 只占未来连续自由日并禁止跨月。
3. 中品灵根标准弟子连续观察三层进度、溢出、圆满后控制练习和日结信息。
4. 对比天灵根普通根骨与中品灵根优秀根骨的吸收、容量和夜间保留差异。
5. 新档立宗选择三门功法各一次，确认宗门只获得所选功法、三名创始弟子自动学习，后续招募弟子不自动学习。
6. 连续执行修炼日，确认个人理解与宗门推演都增长；Mission/心得事件只增长个人理解；切换主修后旧进度保留。
7. 将宗门推演推进到 30，确认只触发一次首次批注、未选择前保持 30 封顶，选择后继续增长；分别核对两种批注的学习条件。
8. 对比不同灵根与三门功法的吸收、炼化、稳定和 AI 行动倾向，确认容量不受功法影响。
9. 检查灵雨、心得、比试与探索事件分别只修改当前灵气、个人功法理解或灵气控制。
10. 资源月结、宗务人日余数、仓库满时损失记录及读档恢复。
