# Cultivation4X 项目当前状态（2026-09-02 更新）

本文档记录世界地图与旧玩法融合、资源生态、弟子个性自主行为、炼气期纳气成长、功法系统、世界自动时间、弟子成长反馈闭环、阶段性修行叙事，以及宗门功能区与部门系统的当前实现。这里是阶段性实现事实，不是冻结架构。

## 1. 概览

- 当前分支：`agent/influence-integration`，远端：`origin`。
- 当前里程碑：弟子按真实月计划与日结结果形成稀疏的阶段性修行叙事，同时保留灵植功能区、部门和成长月报闭环。
- 存档版本：`SaveDataVersion.Current = 25`；拒绝 V24 及更早存档，本版从新档测试。
- V22 自动时间基线已提交并推送为 `8929b3c`；V23 成长反馈、V24 功能区/部门与 V25 经历优化已合并为当前 V25 功能批次。
- 本次没有修改 `WorldGenerator`、`TerrainRenderer`、Camera 或 Unity `timeScale`；更新了实际 UIRoot、弟子、月报和宗门组织页 Prefab，并仅从 SampleScene 的 `PlayerData` 序列化块删除已经不存在的旧设施等级字段。

## 2. 已完成系统

### 2.1 世界地图与资源生态

- `WorldMapData / WorldCell / HexGeometry / Terrain` 是唯一地图数据源；`MapSiteData` 保存地点玩法状态，`WorldLocation` 只作为地图与交互门面。
- `WarehouseData.items` 是物品库存唯一真源，所有增减通过 `WarehouseManager`。
- 地图资源节点每 30 天结算一次，`lastSettledMonth` 防止重复结算；当日刚开发节点从下月开始产出。
- 旧的“每5个宗务人日产出1基础材料”和 `sectDutyWorkCredit` 已删除。宗务日现在从统一行为目录选择真实宗务行为；没有功能区需求时只执行不产资源的日常维护。
- 已开发灵泉不再每日直加修为；它只在弟子执行修炼日时提供环境吸收效率 +10%。

### 2.2 弟子个性与自主行为

- `DiscipleDecisionManager` 在日结后调度，Goal/Utility 评分位于 `DiscipleAIEvaluator`，执行统一桥接到既有 Mission。
- 个性、特质、能力、资源环境和关系目标影响弟子选择；AI 不直接写 Mission 奖励、关系或纳气进度。
- 自主多日 Mission 只能在未来连续自由日足够且不跨月时启动。
- 玩家 Mission 优先于日程；被占用的计划日不补做、不改写，取消 AI Mission 不触发失败惩罚。
- AI 能力评分中的旧资质入口已改为灵根品质；旧角色等级/经验成长入口和 `NPCGrow` 已移除。

### 2.3 炼气期纳气模型

- 练气成长状态真源位于 `CharacterState`：`realmLayer`、`naqiProgress`、`currentAura`、`auraControl`、`fatigue`、`spiritRoot`；个人功法状态由同一角色上的 `mainTechniqueId`、`activeAuxiliaryTechniqueIds`、`techniqueProgresses` 保存。
- 炼气共三层，每层纳气 0–100；溢出自动带入下一层，第三层 100 为炼气圆满。圆满后的修炼日仍可练习灵气控制，但不再增加纳气。
- 灵根负责吸收、泄露与小幅炼化修正：

| 品质 | 吸收倍率 | 夜间泄露 | 炼化倍率 |
|---|---:|---:|---:|
| 驳杂 | 0.2 | 95% | 0.90 |
| 下品 | 0.6 | 90% | 0.95 |
| 中品 | 1.0 | 70% | 1.00 |
| 上品 | 1.8 | 50% | 1.05 |
| 极品 | 3.0 | 30% | 1.10 |
| 天灵根 | 6.0 | 0% | 1.15 |

- 五行亲和度归一化后合计为 1；当前只用于主修功法的柔性适配倍率（0.9–1.1），未扩展元素克制系统。
- 根骨只决定灵气承载。练气一/二/三层基础承载为 50/75/100，再乘孱弱 0.7、普通 1.0、优秀 1.3、上佳 1.6、天生道体 2.0；容量为派生值，不写入存档。
- 只有修炼日吸收环境灵气；所有日子在事件处理后进行夜间泄露，未泄露部分跨日保留。
- 修炼日从打坐炼化、基础练功、灵气运转尝试中稳定加权选择。性格、疲劳与控制力影响选择；运转失败为软失败，只保留 25% 收益。
- 疲劳是软限制，最高只降低约 30% 炼化效率；每日先恢复，再根据行为增加。
- 标准中品灵根、普通根骨、中性功法适配、每月 15 个修炼日的目标仍是 85–100 个日历日完成炼气三层。
- 旧当日灵气归零、大周天、灵气紊乱、调息丹、修炼灵石辅助和 Mission `expReward` 路径已移除。

### 2.4 功法系统 V1

- `Assets/Resources/Configs/Techniques/techniques.json` 是功法定义真源；当前有《青木养气诀》《赤阳锻气诀》《太虚导引篇》三门主修功法，配置包含五行倾向、吸收/炼化/稳定修正和行动标签偏好。
- `PlayerData.techniqueLibrary` 保存宗门功法库、宗门推演进度和批注选择；个人理解进度只保存在对应 `CharacterState.techniqueProgresses`，二者不互相代替。
- 立宗选择功法时，功法进入宗门功法库，并由三名创始弟子学习、装备为主修；以后招募的弟子不会自动学会。
- 个人理解阶段按 0/30/60/90 划分为初学、熟练、精通、圆满。切换主修保留原功法进度；辅修槽目前只保存状态，不产生玩法效果。
- 修炼日同时结算个人理解和宗门推演贡献；Mission/Event 的功法奖励只增加执行者当前主修的个人理解，不增加宗门推演。
- 宗门推演首次到 30 时封顶并触发对应功法的首次批注事件；选择“入门批注”或“因材批注”后解除封顶。60/90 阶段当前自动晋级，不触发额外事件。
- 入门批注在个人理解低于 30 时提供 25% 学习修正；因材批注在对应灵根亲和不低于 0.4 时提供 20% 学习修正。每门功法 V1 只能选择一次首次批注。
- 功法对炼气容量没有影响；主修的吸收、炼化、稳定和五行适配只参与既有纳气公式。旧的单一 `techniqueMastery` 与立宗功法固定属性加成已被替换。
- Utility AI 通过行动标签读取当前主修的偏好评分；没有为功法另建一套 AI 或执行系统。
- 立宗教程中的“传承整理进度”与长期宗门推演是两套不同语义：前者只用于完成立宗引导，后者从 0 开始随实际修炼积累。

### 2.5 30 日循环计划模板

- `PlayerData.monthlyPlanTemplates` 是唯一真源；每个模板保存稳定 ID、名称、30 个循环日程格和绑定弟子 ID。
- 每名弟子最多绑定一个模板，多名弟子可以共享同一模板；未绑定弟子按自由活动执行，删除模板即解除其绑定。
- 第 1/31/61 天均读取模板第 1 格。月计划界面先选择修炼/宗务/自由画笔，再通过单击或按住拖过多个日期连续涂写；不提供整表批量覆盖入口。
- 绑定区默认只显示已绑定弟子；“添加弟子”展开候选列表，选择后加入绑定区，已绑定其他模板的弟子会转绑。
- 手动 Mission 和进行中的 Mission 优先于当日日程，不修改原计划，也不在以后补做。
- AI 多日 Mission 暂时仍不能跨越月末，这是既有任务边界，不由循环模板改变。

### 2.6 事件与展示

- 事件不再直接增加纳气：灵雨增加当前灵气，功法心得增加执行者当前主修的个人理解，比试、探索和修行经历增加灵气控制。
- 日结保存并展示当日最新修炼结果；人生履历只记录失败、进入新层和炼气圆满等里程碑。
- 弟子中心、弟子详情、列表、立宗候选和日结已改为显示灵根、练气层、当前灵气/容量、控制、疲劳与当日行为。
- 全局 HUD 首次绑定主菜单/角色创建状态时不再清空 `UIManager` 面板；只有确实从世界地图离开时才清理旧窗口，避免删档启动后立宗面板被初始化顺序关闭。

### 2.7 世界自动时间 V1

- `TimeManager` 仍是唯一世界时间入口；`CurrentDay` 继续表示“已经完成结算的天数”，正在经历的日期由 `CurrentDay + 1` 派生。
- 日历固定为 30 天/月、12 月/年。新档从第 1 年第 1 月第 1 日 06:00 开始；配置位于 `Assets/Resources/Configs/Time/world_time.json`，当前 ×1/×2/×4 分别为现实 30/15/7.5 秒/日。
- 自动时钟使用 `Time.unscaledDeltaTime`，只在存档初始化完成、宗门已成立且处于正式世界地图流程时运行；读档保留速度选项但默认暂停。
- 06:00 锁定当日弟子日程和稳定功法行为 ID，只生成表现时间段，不提前写入成长；午夜结算读取这份锁定状态。月计划在 06:00 后修改只影响次日。
- Mission 在实际结算和行为展示中优先于锁定月计划；20:00 后统一显示休息。
- 弟子中心只在当前行为段发生变化、06:00 日程锁定、跨日或弟子名单变化时重绘，避免最高速下每个游戏整点重建完整列表。
- 每日日结只写入最新未读摘要，不自动打开模态窗口；顶部“结算”按钮用于查看。第 30 天月结和关键事件会自动暂停，关闭月结或处理事件后仍需玩家手动选择速度继续。
- 暂停原因可叠加；非正式流程、玩家暂停、关键事件、月结待确认和结算失败均不能被速度按钮绕过。普通弟子、宗门、月计划和资源面板不自动暂停世界。
- 存档保存当前小时、速度选项、当日是否已准备、锁定日程、行为 ID 与表现时间段；运行中的暂停状态不保存。

### 2.8 弟子成长反馈闭环 V1

- `DiscipleCurrentStateBuilder` 只读组合角色状态、当日锁定日程和实时 Mission；Mission 或休养会覆盖实际活动显示，但不会改写原计划类型。UI 不保存第二份可写当前状态。
- `NPCManager.ProcessDayWithResults()` 在保留旧 `OnDayPassed()` 入口的同时，为每名实际参与日推进的存活弟子返回执行类型、稳定行为或 Mission ID、结果及未执行原因。
- `DaySettlementSummary.discipleResults` 保存完整弟子日结果；计划日和实际日分别计数，Mission 占用保留原计划证据，跨层纳气使用每日实际正向收益累计。
- `PlayerData.growthFeedback` 是月统计和月报唯一真源：当前月增量统计库存净变化，每30日幂等封存一份月报，最多保留最近12份。
- 现有 `LifeRecord` 已扩展为结构化经历真源，保留旧分类和文本兜底。记录带确定性 ID、类型、Internal/Minor/Major 等级、描述键、来源 ID 和参数；重复结算不会重复写入。
- 日常自主决策只作为 Internal 幂等证据保留2日且不展示；Recent Minor 保留360个已结算日；Chronicle Minor 与 Major 永久保留。普通控制失败不再产生流水账，只记录首次成功、首次失败和控制里程碑。
- 功法理解阶段、练气层级/圆满、死亡/濒死/永久创伤、关键事件、重要关系及配置标记的重大 Mission 进入关键经历；玩家白天处理事件使用 `ActiveDay` 归档。
- 弟子列表展示姓名、境界/纳气和当前实际活动；详情增加“成长”页并将“履历”升级为过滤 Internal 的“经历”。宗门月报提供重点/全员切换，资源库存净变化与月结应产/入库/损失分列。
- 第30日仍只自动暂停、不自动弹窗。顶部“结算”与宗门管理可打开最新月报；关闭最新未确认月报解除 MonthEnd，但仍保持玩家暂停，历史月报关闭不改变时间状态。

### 2.9 宗门功能区与部门系统 V1

- `WorldMapProgressState.functionalZones` 是功能区唯一真源；V1 只开放灵植区。普通格必须已探索、由玩家达到影响级以上控制、可建设且不与宗门核心、`MapSiteData`、资源节点或其他功能区重叠，才能从地图“行动”页规划。
- 功能区稳定 ID 为 `sect_zone_cell_{cellIndex}`，依次经历待开垦、试种地和药圃。开垦累计8点、试种累计10点；药圃照料累计4点后，下一有效宗务人日采收1份 `herb_qingling` 并消耗4点成熟度。
- 每次有效宗务贡献按环境倍率结算：适宜湿润地形1.2、严酷或过度干燥地形0.8、其他1.0。多人按稳定角色ID顺序执行，前一人完成阶段后，后一人可在同日选择下一阶段行为。
- 仓库满时采收成熟度仍被消耗，青灵草损失并进入当日提示。任意阶段撤销功能区都会删除记录，从而永久清空阶段、进度、成熟度和部门绑定。
- `PlayerData.departments` 与 `nextDepartmentSequence` 保存部门。一个存活弟子最多属于一个部门，负责人必须是成员；一个部门可负责多个功能区，一个功能区只能绑定一个部门。删除部门、弟子死亡或成员失效会同步清理引用。
- `ActionDefinition.executionKind` 区分自主 Mission 与非 Mission 宗务；两者共用 `DiscipleAIEvaluator` 的评分、性格、能力和稳定选平逻辑，但执行入口仍分别属于 `DiscipleDecisionManager` 与 `SectDutyResolver/SectDutyExecutor`。
- 所有弟子都能响应全宗门功能区需求；本部门负责区域提供额外倾向而不形成独占。弟子日结果、成长统计和月报保存实际行为ID、目标区域和部门证据。
- 宗门管理页签为“宗门概况 / 弟子 / 功能区 / 部门 / 资源 / 事务”，六个入口复用公共紧凑页签尺寸。功能区和部门主体使用 `SectOrganizationBody.prefab` 的全尺寸滚动布局；功能区页可定位地图，部门页支持创建、改名、成员、负责人、绑定和删除。
- 设施等级、升级接口、升级事件和建设页已删除，改由 `availableFacilities` 表示功能开放。保留功能采用原Lv1固定效果：仓库10类、炼丹3日1份、护山阵将失败伤势从3日降为1日、修炼室开放为1.0倍且未开放为0.8倍。旧秘境任务配置保留为不可分配兼容内容，不再在宗门界面暴露入口。

### 2.10 弟子阶段性修行叙事 V1

- `LifeRecord` 仍是唯一经历真源；新增计划阶段、重复行为倾向、疲劳积累/恢复、普通功法体会和部门变化类型，不保存第二套经历列表。
- `DiscipleMonthlyStats.narrative` 保存当月开始/完成、行为倾向、疲劳与功法叙事的幂等状态；它只解释 `DiscipleDayResult` 和已累计月统计，不重新计算纳气、疲劳、功法收益或行动结果。
- 本月第一次实际完成修炼/宗务时写入阶段开始；当前有效模板中不存在后续同类计划日时写入阶段完成。Mission或休养覆盖的计划日仍保留计划证据，但不会被叙述为实际执行。
- 同一稳定修炼或宗务行为在当月第3次实际执行时形成一次行为倾向；通用 `free_day` 不生成虚构自由活动经历。普通功法体会每月最多一次，功法阶段变化仍是Major。
- 疲劳跨越明显阶段时记录积累，恢复至安全区后记录缓解；结构化参数可保存内部数值，但经历正文不显示纳气、疲劳、功法理解或控制阈值数字。
- Major、阶段完成、行为倾向、功法体会和部门变化进入 Chronicle 长期保留；计划开始、疲劳与普通短期状态属于 Recent。重要性和保留期相互独立，Chronicle Minor不会被月报误判为Major。
- 部门加入、离开和负责人变化从现有宗门组织命令入口写入；弟子死亡清理部门引用时不额外生成“离开部门”，避免与死亡经历重复叙事。
- 经历页继续复用现有时间轴 Prefab，日期改为“第X年·第X月·初X”，Major使用强调色；明确数值仍只出现在成长页和月度统计。

## 3. 时间与数据流

```text
自动时钟逐整点推进
  → 06:00 锁定当天日程与表现行为（不写成长结果）
  → 20:00 发布 DayEnding 通知
  → 24:00 检查关键事件/收件箱阻塞
  → 捕获日结前快照并令 CurrentDay + 1
  → 弟子恢复疲劳/心境并按锁定日程结算（Mission/休养优先；宗务通过统一 Utility 评分选择功能区行为）
  → 地图每日效果
  → MissionManager.ProcessDay 显式推进
  → 外部威胁与 Event
  → 所有存活弟子夜间灵气泄露
  → 每 30 天资源月结
  → DaySettlementSummary
  → DiscipleDecisionManager.ProcessSettledDay 显式决策
  → 增量更新月统计、结构化经历并在月末幂等封存宗门月报
  → 执行一次最终自动存档并发布兼容通知
```

`OnDayPassed` 与 `OnDaySettlementReady` 仍用于兼容 UI/外部通知，不再决定 Mission 和弟子 AI 的核心执行顺序。`EndDay()` 只保留为测试兼容包装；正式玩法不再显示“结束今天”。快速推进统一走相同整点和午夜入口，资源调试不再直接调用 `RestoreDay()`。

## 4. 验证状态

| 项目 | 当前结果 |
|---|---|
| JSON 配置解析 | 73/73 可解析；自主 Mission 与宗务行为引用由 Unity `ConfigValidator` 分类校验 |
| `git diff --check` | 通过；仅有现有换行符提示，无空白错误 |
| 编译 | Assembly-CSharp / Assembly-CSharp-Editor 独立编译均为 0 错误；Unity 完整 EditMode 导入编译通过 |
| EditMode | V25 最终完整回归 531/531 通过；结果为 `Logs/v25-final-post-id-fix-results.xml`，本地 XML 不提交 |
| Play Mode | V25 未执行；只有再次获得桌面控制授权后手测 |

V22 测试证据：`Logs/world-time-speed-results.xml`、`Logs/global-ui-speed-final-results.xml`、`Logs/v22-speed-final-editmode-results.xml`（本地日志，不提交）。V23 基线证据：`Logs/v23-baseline-editmode-results.xml`（完整 512/512）。V24 最终结果：`Logs/v24-final-527-editmode-results.xml`（527/527）。V25 定向证据：`Logs/v25-growth-feedback-final-results.xml`（17/17，含90日叙事密度与ID防碰撞）、`Logs/v25-disciple-ai-final-results.xml`（22/22，含关系写入）、`Logs/v25-sect-zone-results.xml`（11/11）、`Logs/v25-global-ui-results.xml`（29/29）及 `Logs/v25-world-content-results.xml`（21/21）；最终完整结果见 `Logs/v25-final-post-id-fix-results.xml`（531/531）。

## 5. 当前高风险与限制

1. V25 新增经历保留策略、月度叙事幂等状态和疲劳前后证据，并继承V24功能区/部门存档；必须从新档开始，拒绝 V24 及更早存档，不提供迁移。
2. 根骨品质目前由既有 `physique` 数值区间映射，尚无独立根骨品质字段；如果角色属性生成区间变化，必须同步复核容量映射。
3. 85–100 日是标准模板的确定性回归目标，不代表所有性格、灵根、环境与事件组合都落在该区间。
4. 功法五行适配、行动偏好、个人理解速度和宗门 30 阶段事件频率仍需 Play Mode 长周期观察；0.9–1.1 的柔性适配和 30–45 日首次批注目标是首版平衡基线。
5. 月计划已迁移为全局 Prefab Screen；1920×1080、1280×720 的自动布局和最终视觉密度仍需 Play Mode 手测。
6. `NPCData.level/exp` 仍作为旧 ScriptableObject 模板兼容字段存在，但已不参与运行态成长；删除公开序列化字段需单独审查资源引用。
7. 功法管理 UI 当前只展示主修、个人阶段、宗门推演和批注结果；未提供完整换功法/辅修管理界面，辅修也尚未接入实际效果。
8. 60/90 宗门阶段目前只自动晋级，尚无专属内容事件；不得把 V1 行为误当成长期冻结规则。
9. SampleScene 旧按钮/旧物件仍由 `LegacyWorldUiGate` 隐藏；清理时必须同时修改场景与 gate。
10. 午夜链现在显式调用 Mission 和弟子 AI；未来更改顺序时必须检查重复订阅、重复奖励、自动存档次数与失败后的部分状态，不能重新把核心顺序交给事件订阅。
11. 日结失败会以 `SettlementFailure` 暂停，但不会尝试运行态回滚；当前恢复路径是读取午夜前最后一份完整存档，需要 Play Mode 故障注入验证。
12. 自动测试覆盖日历、边界整点、存读档状态、30/31 日月结和 90 日快速推进；不同种子下真实全系统 ×1/×4 长跑一致性与视觉可用性仍需 Play Mode 验证。
13. 月统计只增量消费午夜生成的日结果，不能改回扫描历史文本；任何新增成长入口都必须进入同一日结果或明确写入结构化经历，避免月报漏项。
14. 月报保存的是封存摘要，不能反向成为角色成长或库存真源；资源净变化与月结生产明细必须继续分列，展示时不得二次相加。
15. 当前高亮阈值、最多8名、Recent/Chronicle/Internal保留期及中文模板均是 V1 可调规则，不是长期架构禁令；调整时需同步存档校验和回归测试。
16. 灵植区阈值、环境倍率、部门加成和青灵草产出均为 V1 平衡基线，不是长期架构限制；调整时必须保持功能区唯一真源和午夜单次执行。
17. 部门负责人当前只用于组织展示，不提供额外产出倍率。宗务预览会在06:00展示当时最优候选，午夜仍按稳定顺序重新评估真实阶段，最终统计只采信午夜执行结果。
18. 功能区取消会永久清空全部进度，当前仅使用二次点击确认；仍需 Play Mode 验证误操作防护、地图标记辨识度和定位返回路径。
19. 设施等级已从代码和 SampleScene 序列化块删除；测试中的旧字段 JSON 只用于确认旧版存档被拒绝，不代表运行时兼容入口。
20. 月计划阶段完成按当天午夜看到的有效模板判断；同月完成后再追加未来同类计划，不回写历史，也不生成第二次完成记录。该行为避免重复经历，但需要通过玩法手测确认是否符合玩家预期。
21. 当前自由日没有具体自由行为真源，`free_day` 不生成研读、社交或休息叙事；真实 Mission、Event 与关系变化仍可形成经历，禁止为了填充经历页虚构自由活动结果。
22. 典型21修炼/4宗务/5自由模板的自动测试每月产生5–8条可见经历；这只是调试目标而非硬上限，重大事件不会因为密度目标被丢弃。

## 6. Play Mode 手测清单

1. [部分通过] 新档进入世界地图后默认暂停、06:00 三名弟子生成今日行为；暂停、×1/×2/×4 和普通面板打开时继续流逝均已通过。调速后的 30/15/7.5 秒实际计时待桌面控制可用时复测。
2. [已通过] 已验证午夜跨日、每日结算不弹窗、“结算”按钮打开最新摘要；弟子中心持续打开跨18:00与20:00时，当前行为即时从“游历休整”切换为“自省”和“休息”。
3. [已通过] 使用正常快速推进链到第30日；月结只发生一次并自动暂停，速度按钮无法绕过，确认后仍需手动选择速度继续。
4. 关键事件出现时自动暂停，处理后不会自行继续；关键事件与月结同时存在时需全部解除。
5. [已通过] 14:36 由事件选择触发自动保存，退出再读档后日期、时间、×1 速度选项和第31日锁定行为一致，默认暂停且日结未重复。
6. 新建、复制、重命名和删除模板；绑定三名弟子，点击与拖动修改日程，并确认第 1/31/61 天循环一致；06:00 后修改只影响次日。
7. 手动 Mission 覆盖修炼/宗务日且不补做；AI 多日 Mission 只占未来连续自由日并禁止跨月。
8. 中品灵根标准弟子连续观察三层进度、溢出、圆满后控制练习和日结信息。
9. 对比天灵根普通根骨与中品灵根优秀根骨的吸收、容量和夜间保留差异。
10. 新档立宗选择三门功法各一次，确认宗门只获得所选功法、三名创始弟子自动学习，后续招募弟子不自动学习。
11. 连续执行修炼日，确认个人理解与宗门推演都增长；Mission/心得事件只增长个人理解；切换主修后旧进度保留。
12. 将宗门推演推进到 30，确认只触发一次首次批注、未选择前保持 30 封顶，选择后继续增长；分别核对两种批注的学习条件。
13. 对比不同灵根与三门功法的吸收、炼化、稳定和 AI 行动倾向，确认容量不受功法影响。
14. 检查灵雨、心得、比试与探索事件分别只修改当前灵气、个人功法理解或灵气控制。
15. 资源月结、仓库满时损失记录及读档恢复；确认旧宗务日不再产生基础材料。
16. 探索并控制一个普通可建设格，从地图“行动”页规划灵植区；验证宗门核心、地点、资源节点和外缘格不能规划。
17. 创建“百草堂”，分配两名弟子并绑定区域；对比部门成员与无部门弟子的宗务选择，确认部门只提供倾向而不独占。
18. 推进灵植区经过开垦、试种、照料和采收，确认多人同日跨阶段、环境倍率、青灵草入库及成长反馈中的具体区域行为。
19. 在药圃阶段执行二次确认撤销，确认地图标记、负责部门、阶段进度和成熟度全部消失。
20. 检查宗门六个页签在1920×1080与1280×720下的滚动、部门编辑和功能区定位；确认不再出现设施等级、升级按钮和旧秘境入口。
21. 对比修炼室未开放/已开放时0.8/1.0纳气倍率，并回归仓库、炼丹、护山阵、任务堂和立宗路线开启功能。
22. 使用21修炼/4宗务/5自由模板运行90天，确认每月第一次实际修炼/宗务和最后一个计划日分别形成开始/完成经历，Mission或休养覆盖时不虚构完成。
23. 在同月反复执行打坐、基础练功、控灵和灵植宗务，确认行为倾向、功法体会与疲劳积累/恢复稀疏出现，正文不显示成长数值。
24. 加入、离开部门及更换负责人后检查经历时间轴；确认日期显示为年月日、Major卡片有强调、读档后不重复。
25. 普通月份观察目标约3–8条，事件丰富月份允许超过；确认自由日占位行为不会生成虚构研读、交谈或休息经历。
